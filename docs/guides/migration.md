# Migration Guide: Configurable Storage Backend

このガイドでは、既存のTaskManagerセットアップを新しい設定可能なストレージシステムに移行する方法を説明します。

## 概要

v1.1.0以降、TaskManagerは2種類のストレージバックエンドをサポートしています：
- **JSONファイルストレージ**: シンプルで軽量。データベース不要。
- **PostgreSQLデータベース**: 既存の実装。複数インスタンス間でのデータ共有に適しています。

## 既存ユーザー向け移行手順

### ステップ1: 設定ファイルの作成

プロジェクトルートに`config.toml`を作成します：

```toml
[storage]
type = "database"
url = "${DATABASE_URL}"
```

これにより、既存のデータベースベースのセットアップが引き続き動作します。

### ステップ2: アプリケーションの更新

```bash
git pull origin main
cargo build --release
```

### ステップ3: 動作確認

```bash
cargo run
```

アプリケーションが正常に起動し、既存のタスクが表示されることを確認してください。

## ストレージタイプの切り替え

データベースとJSONストレージ間で切り替えたい場合、アプリケーションは自動的にデータ移行を行います。

### 自動移行の仕組み

アプリケーションは起動時にストレージタイプの変更を検出し、自動的にデータを移行します：

1. **データベース → JSON**: すべてのタスク、ノート、作業ログがJSONファイルにエクスポートされます
2. **JSON → データベース**: JSONファイルのデータがデータベースにインポートされます

### ステップ1: 設定ファイルの更新

例えば、データベースからJSONに移行する場合、`config.toml`を以下のように変更：

```toml
[storage]
type = "json"
path = "~/task-manage/data.json"
```

### ステップ2: アプリケーションの起動

```bash
cargo run
```

アプリケーションは自動的に：
- ストレージタイプの変更を検出
- 以前のストレージからデータを読み込み
- 新しいストレージにデータを移行
- 移行完了のメッセージを表示

### 注意事項

- 移行は初回起動時に一度だけ実行されます
- 移行元のストレージに接続できない場合、警告が表示されますが、アプリケーションは新しいストレージで起動します
- 移行前のデータは保持されるため、元のストレージに戻すことも可能です

## 新規ユーザー向け

新規ユーザーは、JSONストレージから始めることをお勧めします：

1. `config.toml.example`を`config.toml`にコピー
2. 必要に応じて設定を調整
3. アプリケーションを起動

## トラブルシューティング

### 設定ファイルが見つからない

エラーメッセージ: `Warning: Failed to load config.toml`

**解決策**: アプリケーションはデフォルトでJSONストレージ（`~/task-manage/data.json`）にフォールバックします。`config.toml`を作成して明示的に設定してください。

### データベース接続エラー

`config.toml`でデータベースを指定している場合、`DATABASE_URL`環境変数が正しく設定されていることを確認してください。

### データが表示されない

1. ストレージタイプが正しいか確認
2. JSONの場合、`~/task-manage/data.json`が存在するか確認
3. データベースの場合、接続情報が正しいか確認
4. `s`キーを押して再同期を試行

## 設定例

### 開発環境（JSON）

```toml
[storage]
type = "json"
path = "~/task-manage/data.json"
```

### 本番環境（データベース）

```toml
[storage]
type = "database"
url = "${DATABASE_URL}"
```

### カスタムJSONパス

```toml
[storage]
type = "json"
path = "/path/to/custom/tasks.json"
```

## サポート

問題が発生した場合は、GitHubのIssuesで報告してください。
